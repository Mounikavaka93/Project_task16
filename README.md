# Verdant — Nature SaaS Landing Page

Modern nature-intelligence landing page built with React, Vite, and Tailwind CSS.

## Run locally

```bash
npm install
npm run dev
```

Then open the local URL printed in the terminal.

## Build

```bash
npm run build
npm run preview
```
